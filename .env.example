# Copy to .env and fill in as needed.
# Only required if using hosted inference instead of local HF transformers.

TOGETHER_API_KEY=
FIREWORKS_API_KEY=

# Optional: HF token for gated models / higher rate limits
HF_TOKEN=
